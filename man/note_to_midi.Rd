% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pitch.R
\name{note_to_midi}
\alias{note_to_midi}
\alias{midi_to_note}
\alias{midi_to_freq}
\title{Convert between note names and MIDI note numbers}
\usage{
note_to_midi(x)

midi_to_note(x)

midi_to_freq(x)
}
\arguments{
\item{x}{For \code{note_to_midi()}, a character vector of note names such as
\code{"C4"}, \code{"F#3"} or \code{"Bb5"}. Numbers are passed through unchanged. For
\code{midi_to_note()} and \code{midi_to_freq()}, MIDI note numbers.}
}
\value{
\code{note_to_midi()} returns integers, \code{midi_to_note()} returns note
names (always spelled with sharps), and \code{midi_to_freq()} returns
frequencies in hertz.
}
\description{
MIDI numbers pitches from 0 to 127, with middle C (\code{"C4"}) at 60 and
concert A (\code{"A4"}, 440 Hz) at 69. Note names use a letter, an optional
sharp (\verb{#}) or flat (\code{b}), and an octave number.
}
\examples{
note_to_midi(c("C4", "A4", "F#3", "Bb5"))
midi_to_note(60:64)
midi_to_freq(69)
}
