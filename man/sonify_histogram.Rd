% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/histogram.R
\name{sonify_histogram}
\alias{sonify_histogram}
\title{Hear a distribution, like a histogram}
\usage{
sonify_histogram(
  data,
  x,
  weight = NULL,
  bins = NULL,
  binwidth = NULL,
  instrument = "piano",
  length = 20,
  bpm = NULL,
  log = FALSE,
  scale = "pentatonic",
  key = "C",
  range = c("C3", "C6"),
  engine = c("auto", "synth", "fluidsynth"),
  force = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{A column of numbers whose distribution you want to hear.}

\item{weight}{Optional. A column of counts, for data that's already counted.}

\item{bins}{Number of bins. Use \code{bins} or \code{binwidth}, not both. If you give
neither, soundeR uses 30 bins and suggests picking a \code{binwidth}.}

\item{binwidth}{How wide each bin is, in the units of \code{x}. Bins line up
with multiples of \code{binwidth}, so \code{binwidth = 10} for years gives 1900 to
1909, 1910 to 1919, and so on.}

\item{instrument}{The instrument to use (see \code{\link[=instruments]{instruments()}}). With more
than one voice, one instrument per voice, in order or named by voice.
Voices you leave out play the piano.}

\item{length}{Total length in seconds of the sweep.}

\item{bpm}{Notes (bins) per minute. Use instead of \code{length}.}

\item{log}{If \code{TRUE}, pitch follows the logarithm of the counts. That
spreads out the notes when a few bins are much bigger than the rest.}

\item{scale}{The musical scale that pitches snap to. See \code{\link[=sound_scales]{sound_scales()}}.
The default, \code{"pentatonic"}, sounds pleasant with any data. With
\code{"none"}, the built-in synth plays exact frequencies, but real
instruments play the nearest semitone.}

\item{key}{The key of the scale, like \code{"C"}, \code{"G"} or \code{"Bb"}.}

\item{range}{The lowest and highest notes to use, like \code{c("C3", "C6")}.}

\item{engine}{How to make the sound. \code{"auto"} uses real instruments when
the fluidsynth package and a soundfont are available, and the built-in
synth otherwise. See \code{\link[=sound_setup]{sound_setup()}}.}

\item{force}{Set to \code{TRUE} to allow sonifications longer than 20 minutes.}
}
\value{
A \code{sonification} object. Print it to hear it, or pass it to
\code{\link[=sonify_video]{sonify_video()}} to see it as a histogram.
}
\description{
\code{sonify_histogram()} is the sound version of \code{ggplot2::geom_histogram()}.
It sorts a column of numbers into bins, then sweeps across them from the
lowest bin to the highest. Each bin is one note: the more rows in the bin,
the higher (and louder) the note. Empty bins are silent.
}
\details{
Like a histogram, what you hear depends on the bins. Try a few values of
\code{binwidth}: narrow bins show detail (and noise), wide bins show the overall
shape.

If your data is already counted, with one row per value and a column of
counts, give that column to \code{weight}.

\code{\link[=notes]{notes()}} on the result has one row per non-empty bin: \code{bin_start} and
\code{bin_end} give its edges (each bin includes its start but not its end),
\code{value} is its count, and \code{row} is the bin's number, counting from the
lowest.
}
\examples{
# Already-counted data: houses built in Nebraska, by year
ne_house_years

# One bin per decade
ne_house_years |>
  sonify_histogram(year_built, weight = houses, binwidth = 10, instrument = "marimba")

# Raw values work too
sonify_histogram(data.frame(x = rnorm(500)), x, bins = 20, instrument = "bell")
}
