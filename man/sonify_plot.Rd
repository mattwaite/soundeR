% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/video.R
\name{sonify_plot}
\alias{sonify_plot}
\title{Draw a sonification as a chart}
\usage{
sonify_plot(
  x,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  x_label = NULL,
  y_label = NULL,
  theme = NULL,
  title_position = c("plot", "panel"),
  point_color = NULL
)
}
\arguments{
\item{x}{A sonification made by \code{\link[=sonify_data]{sonify_data()}}, \code{\link[=sonify_histogram]{sonify_histogram()}} or
\code{\link[=sonify_density]{sonify_density()}}.}

\item{title, subtitle, caption}{Optional text for the chart.}

\item{x_label, y_label}{Axis labels. By default, the names of the columns you
mapped.}

\item{theme}{Optional. A ggplot2 theme, complete (like
\code{ggplot2::theme_classic()}) or partial (like
\code{ggplot2::theme(plot.title = ggplot2::element_text(size = 24))}). It's
added last, so it overrides soundeR's defaults.}

\item{title_position}{\code{"plot"} (the default) lines titles and captions up
with the whole image; \code{"panel"} lines them up with the plot panel.}

\item{point_color}{Color for the dots (or bars, or curve). With more than
one voice, one color per voice, in order or named by voice; by default
each voice gets its own color from a colorblind-friendly palette.}
}
\value{
A ggplot.
}
\description{
\code{sonify_plot()} draws the chart that \code{\link[=sonify_video]{sonify_video()}} animates, as an
ordinary ggplot, with every note played. Print it for a picture of your
sonification, or change it the way you'd change any ggplot (add \code{labs()},
scales, annotations or a theme) and pass it to \code{sonify_video()} with
\verb{plot =}. The video adds the moving playhead on top.
}
\details{
The chart depends on how you made the sonification:
\itemize{
\item From \code{\link[=sonify_histogram]{sonify_histogram()}}, a histogram. From \code{\link[=sonify_density]{sonify_density()}}, a
density curve.
\item With \code{sequence}, each group gets its own row, like the New York Times's
2010 Olympic Musical.
\item Otherwise, \code{pitch} (up the side) against \code{time} or row order (along the
bottom).
}

Titles and captions line up with the edge of the whole image, not the
plot panel (\code{plot.title.position = "plot"}), even with a complete theme
given to \code{theme}. Use \code{title_position = "panel"} for ggplot2's default. If
you add a complete theme to the result yourself, with \code{+}, ggplot2 resets
the title position, so add \code{ggplot2::theme(plot.title.position = "plot")}
after it. \code{\link[=sonify_video]{sonify_video()}} lines titles up for you either way.
}
\examples{
\dontrun{
race <- luge_finals |>
  sonify_data(time = behind, time_scale = 1, sequence = event, instrument = "piano")

# A picture of the sonification
sonify_plot(race)

# Customize it like any ggplot, then animate it
p <- sonify_plot(race) +
  ggplot2::labs(title = "Fractions of a second", x = "Seconds behind the winner") +
  ggplot2::scale_x_continuous(labels = function(s) paste0("+", s, " s"))
sonify_video(race, "luge.mp4", plot = p)
}
}
