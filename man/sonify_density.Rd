% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density.R
\name{sonify_density}
\alias{sonify_density}
\title{Hear a distribution as a smooth curve}
\usage{
sonify_density(
  data,
  x,
  weight = NULL,
  adjust = 1,
  glide = TRUE,
  instrument = if (glide) "triangle" else "piano",
  length = 10,
  points = 120,
  scale = "pentatonic",
  key = "C",
  range = NULL,
  pan = FALSE,
  engine = c("auto", "synth", "fluidsynth")
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{A column of numbers whose distribution you want to hear.}

\item{weight}{Optional. A column of counts, for data that's already counted.}

\item{adjust}{Make the curve smoother (bigger) or more detailed (smaller).
\code{2} is twice as smooth as the default.}

\item{glide}{If \code{TRUE} (the default), one continuous gliding tone. If
\code{FALSE}, a run of notes, which works with any instrument.}

\item{instrument}{The sound to use. With \code{glide = TRUE}, \code{"sine"},
\code{"triangle"} or \code{"square"}.}

\item{length}{Total length in seconds of the sweep.}

\item{points}{How many points along the curve to use.}

\item{scale}{The musical scale notes snap to when \code{glide = FALSE}. See
\code{\link[=sound_scales]{sound_scales()}}. The glide always follows the curve exactly.}

\item{key}{The key of the scale, like \code{"C"}, \code{"G"} or \code{"Bb"}.}

\item{range}{The lowest and highest notes to use, like \code{c("C3", "C6")}. By
default, the usual range of the instrument (see \code{\link[=instruments]{instruments()}}), or the
range several instruments share. Built-in sounds use C3 to C6.}

\item{pan}{If \code{TRUE}, the sound travels from your left speaker to your
right as the sweep moves across the curve.}

\item{engine}{How to make the sound. \code{"auto"} uses real instruments when
the fluidsynth package and a soundfont are available, and the built-in
synth otherwise. See \code{\link[=sound_setup]{sound_setup()}}.}
}
\value{
A \code{sonification} object. Print it to hear it, or pass it to
\code{\link[=sonify_video]{sonify_video()}} to see the curve.
}
\description{
\code{sonify_density()} is the sound version of \code{ggplot2::geom_density()}. It
draws a smooth curve through the distribution of a column, then sweeps
across it from the lowest value to the highest. By default you hear one
continuous tone that glides up where the data is dense and down where it's
sparse, and gets quieter where there's little data.
}
\details{
\code{adjust} controls how smooth the curve is, just like in \code{geom_density()}:
bigger values smooth more, smaller values show more detail.

The glide uses soundeR's built-in sounds (\code{"sine"}, \code{"triangle"} or
\code{"square"}), because real instruments play separate notes. With
\code{glide = FALSE}, the curve is played as a quick run of notes instead, and
any instrument works.

\code{\link[=notes]{notes()}} on the result has one row per point along the curve: \code{x_value}
is where the point sits, \code{value} is the curve's height there.
}
\examples{
sonify_density(data.frame(x = c(rnorm(300), rnorm(100, 4))), x)

# Already-counted data
ne_house_years |>
  sonify_density(year_built, weight = houses, adjust = 4)
}
